BoxLayout:
    BoxLayout:
        orientation: 'vertical'
        id: labels_container
        spacing: 10  # Adds space between labels for better readability
        padding: 10  # Adds padding around the container